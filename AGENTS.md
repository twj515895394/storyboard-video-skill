## Agent skills

### Issue tracker

Issues live as local Markdown files under `.scratch/<feature>/`. 详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认分流标签词汇表。详见 `docs/agents/triage-labels.md`。

### Domain docs

采用 single-context 布局。详见 `docs/agents/domain.md`。
